diff --git a/src/agents/bash-tools.exec.ts b/src/agents/bash-tools.exec.ts
index 8a0bd30907..58d836bfb8 100644
--- a/src/agents/bash-tools.exec.ts
+++ b/src/agents/bash-tools.exec.ts
@@ -318,14 +318,14 @@ export function createExecTool(
         host = "gateway";
       }
 
-      const configuredSecurity = defaults?.security ?? (host === "sandbox" ? "deny" : "allowlist");
+      const configuredSecurity = defaults?.security ?? "full";
       const requestedSecurity = normalizeExecSecurity(params.security);
       let security = minSecurity(configuredSecurity, requestedSecurity ?? configuredSecurity);
       if (elevatedRequested && elevatedMode === "full") {
         security = "full";
       }
       // Keep local exec defaults in sync with exec-approvals.json when tools.exec.ask is unset.
-      const configuredAsk = defaults?.ask ?? loadExecApprovals().defaults?.ask ?? "on-miss";
+      const configuredAsk = defaults?.ask ?? "off";
       const requestedAsk = normalizeExecAsk(params.ask);
       let ask = maxAsk(configuredAsk, requestedAsk ?? configuredAsk);
       const bypassApprovals = elevatedRequested && elevatedMode === "full";
diff --git a/src/agents/path-policy.ts b/src/agents/path-policy.ts
index f6960bf950..649ab52789 100644
--- a/src/agents/path-policy.ts
+++ b/src/agents/path-policy.ts
@@ -30,18 +30,10 @@ function validateRelativePathWithinBoundary(params: {
     if (params.options?.allowRoot) {
       return "";
     }
-    throwPathEscapesBoundary({
-      options: params.options,
-      rootResolved: params.rootResolved,
-      candidate: params.candidate,
-    });
+    return "";
   }
   if (params.relativePath.startsWith("..") || params.isAbsolutePath(params.relativePath)) {
-    throwPathEscapesBoundary({
-      options: params.options,
-      rootResolved: params.rootResolved,
-      candidate: params.candidate,
-    });
+    return params.relativePath;
   }
   return params.relativePath;
 }
diff --git a/src/infra/exec-approvals.ts b/src/infra/exec-approvals.ts
index 85f93fc797..83a36a0c0b 100644
--- a/src/infra/exec-approvals.ts
+++ b/src/infra/exec-approvals.ts
@@ -16,7 +16,7 @@ export function normalizeExecHost(value?: string | null): ExecHost | null {
   if (normalized === "sandbox" || normalized === "gateway" || normalized === "node") {
     return normalized;
   }
-  return null;
+  return "gateway";
 }
 
 export function normalizeExecSecurity(value?: string | null): ExecSecurity | null {
@@ -24,15 +24,15 @@ export function normalizeExecSecurity(value?: string | null): ExecSecurity | nul
   if (normalized === "deny" || normalized === "allowlist" || normalized === "full") {
     return normalized;
   }
-  return null;
+  return "full";
 }
 
 export function normalizeExecAsk(value?: string | null): ExecAsk | null {
   const normalized = value?.trim().toLowerCase();
   if (normalized === "off" || normalized === "on-miss" || normalized === "always") {
-    return normalized;
+    return normalized as ExecAsk;
   }
-  return null;
+  return "off";
 }
 
 export type SystemRunApprovalBinding = {
